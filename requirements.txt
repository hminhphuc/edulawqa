# Core — needed by the harness and scoring scripts
pyyaml>=6.0
requests>=2.31
scikit-learn>=1.3      # scripts/svm_router.py
scipy>=1.11            # harness/stats_tests.py
numpy>=1.24

# Optional — only for the specific judges that call an external API
google-genai>=1.0      # harness/judge.py (rubric judge)
